# List Done — V2

Catatan pekerjaan yang selesai di branch `v.2.0.0`. Format sama dengan
[list_done.md](list_done.md):

`tanggal pengerjaan - nama feat - git commit`

Riwayat fase 1–21 dan fix 1–18 tetap di [list_done.md](list_done.md) — dokumen ini
mulai dari titik branch v2 berdiri sendiri, supaya riwayat v1 yang sudah jalan di
mesin client tidak tercampur dengan pekerjaan yang belum dirilis ke sana.

---

- 2026-08-24 - fase 21: lisensi & aktivasi, kode dikunci ke satu komputer - 3b597fe — aplikasi terkunci sampai kode lisensi dimasukkan, kode diterbitkan website penjualan (`../web-kios-pos`) dan diverifikasi sepenuhnya offline oleh `src-tauri/src/lisensi.rs` memakai public key Ed25519 yang ditanam di binary sehingga kasir tidak pernah butuh internet untuk urusan lisensi; kode dikunci ke satu ID Mesin lewat field `m` yang ikut ditandatangani, jadi kode yang disalin ke komputer lain ditolak — tanpa itu kode hanya mengikat pemasangan bukan komputer, dan komputer kedua yang menerima salinan akan mengikat ke dirinya sendiri lalu berhasil karena tidak ada yang menghitung; karena ID Mesin belum diketahui saat orang membayar, aktivasinya dua langkah (aplikasi menampilkan ID Mesin, pembeli menukarnya di `/aktivasi` pada website bersama nomor pesanan, boleh dari HP); ID Mesin diambil dari `machine-uid` (MachineGuid Windows / `/etc/machine-id` Linux / IOPlatformUUID macOS) lalu di-hash SHA-256 bersama garam dan ditampilkan 16 hex berkelompok empat, dengan ID acak yang disimpan di `mesin.id` sebagai cadangan kalau OS tidak memberi apa-apa; lisensi disimpan di `app_config_dir/lisensi.json` BUKAN di `pos.db` karena kalau ikut di database, Restore dari mesin lain membawa lisensi mesin itu dan "Buat Baru" menghapus lisensi yang sah; tanda tangan diperiksa ulang tiap aplikasi dibuka bukan cuma saat aktivasi sehingga mengedit `lisensi.json` dengan tangan tidak menghasilkan apa-apa; `/database-manager` sengaja tetap terbuka tanpa lisensi supaya data pemilik toko tidak jadi sandera; tier ikut ditandatangani dan tampil di Pengaturan > Umum bersama tombol Lepas Lisensi yang dikunci master password Database Manager, penguncian fitur per tier belum dipasang; [lisensi.md](lisensi.md) jadi sumber kebenaran format yang dipakai dua repo, [desain.excalidraw](desain.excalidraw) dapat diagram arsitekturnya

- 2026-09-20 - merge master ke v.2.0.0 (laba, mode stok ketat, hak akses laporan, workflow release) - 8b58fe3 — menggabungkan fitur yang sudah jalan di produksi ke branch lisensi: harga beli & laba, mode stok ketat, hak akses laporan kasir, edit user, workflow draft Release, serta migration 0005–0007; dari 7 file yang bertabrakan hanya 2 yang perlu diselesaikan dengan tangan karena sisanya menyentuh bagian berbeda — `docs/list_done.md` (entri kedua branch dipertahankan dan diurutkan tanggal, satu entri fase 21 duplikat bertanda "belum di-commit" dibuang karena isinya sama dengan versi yang sudah punya hash) dan `pengaturan/+page.svelte` (import, state, dan CSS kedua sisi digabung; `type Tab` memakai gabungan penuh `umum/produk/laporan/user/sinkronisasi/keamanan` supaya tab Produk & Laporan dari master tidak hilang tertimpa versi lisensi yang lebih sempit); merge naif sempat menghasilkan CSS rusak — rule `.catatan` kehilangan kurung penutup karena git memotong blok konflik di tengah rule dan penutupnya ada di bagian yang dipakai bersama, ketahuan dari `npm run check` bukan dari membaca diff

- 2026-09-20 - navigasi pindah ke top bar, halaman Produk pakai panel geser - 98c2fbe — sidebar 260px diganti top bar setinggi 60px karena untuk POS navigasi yang dipakai sesekali tidak layak memakan seperlima lebar layar permanen: kasir membuka Penjualan sepanjang hari dan jarang berpindah halaman, sementara tabel produk & laporan justru kekurangan ruang; Pengaturan & Keluar turun ke dropdown avatar dengan versi app ikut di dalamnya supaya tetap sekali klik untuk memeriksa build mana yang terpasang di mesin client; tinggi top bar dijadikan variabel `--topbar-h` karena kasir & produk menghitung tinggi panelnya sendiri dan sebelumnya meng-hardcode `56px` di tiga tempat yang langsung meleset begitu tingginya berubah; latar logo tidak lagi `--accent` karena logonya sendiri hijau sehingga menyatu dengan kotaknya, diganti putih di kedua tema (bukan `--surface`, yang di mode gelap mengembalikan masalah kontras yang sama) dengan border tipis supaya kotak putih itu tidak menyolok di atas permukaan gelap; halaman Produk direstrukturisasi — form & log aktivitas keluar dari kolom kiri tetap dan jadi panel geser 380px sehingga daftar produk yang dipakai sehari-hari mendapat lebar penuh, dengan `refocusBarcode` dihentikan selagi panel tertutup (kalau tidak, scan kasir mendarat di kolom barcode yang tidak dirender), fokus dipasang setelah panel render lewat `setTimeout(0)` bukan sebaris dengan `panelForm = true` saat elemennya belum lahir, Esc menutup berlapis (import > panel form > panel log > menu baris), dan simpan dibedakan: Edit menutup panel karena tugas sekali selesai sedangkan Tambah membiarkannya terbuka karena input barang biasanya berturut-turut sambil membongkar belanjaan; tabel dirapikan dengan badge stok (Habis / n sisa / aman / em dash untuk yang tidak dihitung — stok 0 diberi warna paling kuat karena selama mode stok ketat menyala produk itu tidak bisa dijual sama sekali dan itu harus terlihat saat memindai daftar, bukan baru ketahuan waktu kasir melayani pembeli), angka rata kanan dengan `tabular-nums` supaya satuan ribuan berbaris lurus, "belum diisi" jadi em dash supaya tidak merusak perataan, dan Edit/Hapus jadi menu titik-tiga yang `position: fixed` digambar di luar tabel — sebagai `absolute` ia terpotong `overflow` milik `.list-table-wrap` sehingga baris terakhir harus digulung dulu untuk melihat menunya, dan itu tidak bisa ditolong `z-index` karena elemen yang terpotong overflow tetap terpotong berapa pun z-index-nya; menu membalik ke atas kalau ruang di bawahnya kurang, serta tertutup saat tabel digulung (elemen `fixed` tidak ikut bergerak, jadi akan menggantung lepas dari barisnya) atau saat barisnya hilang dari hasil pencarian; `--warning` ditambahkan ke kedua tema karena sebelumnya dipakai Pengaturan lewat fallback inline tanpa pernah didefinisikan
